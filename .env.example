DATABASE_URL="file:./data/familily.db"
NEXTAUTH_SECRET="generate-a-secure-secret-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Optional: Cron-Job Sicherheit
# Wenn gesetzt, muss der Cron-Job mit "Authorization: Bearer <secret>" aufgerufen werden
CRON_SECRET=""

# Optional: Email (Phase 2)
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
